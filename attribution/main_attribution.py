import argparse
import sys
from pathlib import Path

def main(args):



if __name__ == "__main__":
    p = argparse.ArgumentParser()
    p.add_argument("--compute-means", )
    args = p.parse_args()
    main(args)